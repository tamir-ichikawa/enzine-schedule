# 本番Hostingプレビュー確認計画

## 目的と境界

本番サイトを置き換えず、期限付きFirebase Hostingプレビューチャンネルで新画面を確認します。

- 対象プロジェクトとHostingサイトは `enzine-schedule` に固定する
- Hostingだけの専用設定 `firebase.production.hosting.preview.json` を使う
- 公開元は許可リストで生成した `.firebase-deploy/public` の24ファイルだけにする
- プレビューチャンネルは1日で失効させ、`live` チャンネルには反映しない
- `--no-authorized-domains` を使い、Firebase Authenticationの承認済みドメインを変更しない
- Firestoreルール、Functions、Authenticationユーザー、本番データは変更しない
- 元の `firebase.json` と `.firebaserc` は変更しない

プレビューURLは一時的でも外部から到達できます。URLを公開せず、確認担当者だけで扱います。また、プレビュー画面は本番Authentication・Firestore・Functionsへ接続するため、架空アカウントの作成や会社・事業所・ユーザーの更新は行いません。

## 作成前の停止条件

次のいずれかが満たせない場合はプレビューを作成しません。

1. 公開候補24ファイルの許可リスト監査が合格している
2. テストとFunctions構文検査がすべて合格している
3. 本番Functionsが候補11件と一致している
4. `system/maintenanceMode.active=false` を読み取り確認できる
5. 既存の本番Hostingサイトとプレビューチャンネルを読み取り確認できる
6. `firebase.json` と `.firebaserc` に差分がない
7. 外部公開状態を作る操作について、その回の明示承認がある

## 2026年8月2日の準備確認

- 公開候補: 24ファイル。秘密情報、Functions、Firestoreルール、開発用ファイルを含まない
- 自動テスト: 24件すべて合格。Functions構文検査も合格
- 本番Hosting: 固定サイト `enzine-schedule` の1件だけ
- 本番Hostingチャンネル: `live` の1件だけ。既存プレビューチャンネルなし
- 本番Functions: 候補と一致する12件。追加・欠落なし
- 本番所属データ: 必須補完なし、警告なし
- メンテナンス: 解除状態
- 既存設定: `firebase.json` と `.firebaserc` に差分なし

上記は読み取り監査まで完了しています。現在の停止位置は、1日限定プレビューチャンネルを作成する明示承認の直前です。

## 2026年8月2日のプレビュー作成結果

- チャンネル: `multi-office-20260802`
- 失効: 2026年8月3日18時42分頃（日本時間）
- 公開ファイル: 24件
- 配信確認: HTTP 200、配信 `index.html` と候補ファイルのSHA-256が一致
- ヘッダー確認: `Cache-Control: no-store, max-age=0`、`nosniff`、`DENY`、`no-referrer`、カメラ・マイク・位置情報無効、検索登録拒否
- `live` チャンネルの更新時刻: 変更なし
- Auth承認済みドメイン: 同期しないオプションで作成
- Firestoreルール、本番 `live` Hosting: 変更なし
- Functions: 本人所属の事業所名だけを返す `getCurrentOfficeBrand` 1件を承認後に追加。本番12件と候補が一致
- 本番データ: 正式な運営会社名・事業所名・事業所管理者1件を正式データとして維持

ヘッダー修正版では `admin.html`、`admin.js?v=101`、`ui-common.js?v=84` が候補と一致することをHTTP 200で確認しました。現在の停止位置は、事業所管理者による上段・下段表示の再確認待ちです。

### 利用者による再確認結果

2026年8月2日、正式な事業所管理者アカウントで次の表示を確認し、問題なしとの報告を受けました。

- 上段: `エンジンルーム千葉 スケジュール`
- 下段: `事業所管理者`
- その他の事業所管理者向け表示制限: 正常

期限付きプレビュー確認は合格です。現在の停止位置は、本番 `live` Hosting反映前の直前監査と明示承認待ちです。

## プレビュー作成後に利用者へ依頼する確認

最初は表示と既存データの読み取りだけを確認します。

1. 通常ログイン画面が開き、事業所ロゴが未設定ならロゴ欄が非表示になる
2. 既存の全体管理者でログインできる
3. ヘッダーが「事業所名 スケジュール」になる。事業所名が未登録の場合は互換表示になる
4. 全体管理者の「登録ユーザー」に会社・事業所フィルターが表示される
5. 事業所管理者には「既存UIDを紐づけ」が表示されない
6. 事業所管理者には運営会社・事業所・事業所管理者発行の管理欄が表示されない
7. 課金安全チェックは全体管理者だけに表示される
8. 支援員・利用者の既存主要画面が読み取れる

この確認中は会社名・事業所名、所属、アカウント、メンテナンス状態を変更しません。更新操作、メンテナンス切替、別事業所データの拒否確認は、Firestoreルールを含む次段階の試験計画で個別承認を得て実施します。

## 異常時

- プレビューURLの利用を中止する
- プレビューチャンネルを期限切れまで使用しない
- 本番の通常URLでは同じ異常がないことを読み取り中心に確認する
- `live` Hosting、Firestoreルール、本番データには触れずに原因を切り分ける

プレビューは `live` を置き換えないため、本番Hostingのロールバック操作は不要です。誤って本番URLへ反映された場合だけ、Firebase Hostingのリリース履歴から直前の正常リリースへ戻します。
