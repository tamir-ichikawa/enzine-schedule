# 画面デザイン：Ocean Blue / Muted Sage

## 目的

既存機能と権限制御を変えずに、2種類の画面デザインを切り替えられます。Ocean BlueはPCでページメニューを含む左サイドバー、右上の設定メニュー、白いカードを中心に構成しています。Muted Sageは従来の柔らかい緑系デザインです。

## 切り替え方法

1. ログイン後、画面右上の歯車を開きます。
2. 「画面デザイン」で「Ocean Blue」または「Muted Sage」を選びます。
3. 選択はそのブラウザー・その端末だけに保存されます。

保存済みの選択がない端末ではOcean Blueが初期表示です。Muted Sageへいつでも切り替えられます。Firestore、Authentication、FunctionsにはUIの選択を保存しません。したがって、別の利用者、別の事業所、本番データ、権限判定には影響しません。

## 今回変更していないもの

- 各役割の機能、画面内の項目、登録・更新処理
- 全体管理者、事業所管理者、支援員、利用者の権限制御
- Firebase設定、Firestoreデータ、Functions、Firestoreルール
- ロゴや事業所名の取得方法

## 確認の目安

全体管理者、事業所管理者、支援員、利用者の各画面で、次を確認します。

- 右上の歯車から両UIを切り替えられる
- ページを再読み込みしても同じUIが選ばれる
- Muted Sageへ切り替えると以前の配置になる
- PC幅では左サイドバー、スマートフォン幅では上部ヘッダーになる
- 既存のタブ、ボタン、登録・更新操作が変わらず使える

Muted Sageの削除は、Ocean Blueで4役割の実機確認が完了した後に別作業として判断します。
