# マルチ事業所対応のローカル実装範囲

この文書は、1つの共通アプリを将来の「運営会社 → 事業所 → 事業所管理者・支援員・利用者」へ拡張するために、ローカルFirebase Emulator上へ追加した土台をまとめたものです。本番の千葉Firebase、本番データ、本番公開環境への変更やデプロイは行いません。

## データ構造

| コレクション | 主な項目 | 用途 |
| --- | --- | --- |
| `organizations/{organizationId}` | `name`, `active`, `groupId` | 運営会社。互換用の `groupId` は会社IDと同じ値を保存 |
| `offices/{officeId}` | `organizationId`, `groupId`, `name`, `active` | 運営会社に所属する事業所 |
| `users/{uid}` | `role`, `accessScope`, `organizationId`, `officeId`, `groupId` | 管理者・支援員・利用者の所属と操作範囲 |
| `system/activeUsers_{officeId}` | `organizationId`, `officeId`, `users` | Firestoreルールで分離できる事業所別のActive利用者キャッシュ |
| `system/activeUsers` | 従来の `users` と `offices.{officeId}.users` | 既存画面との互換用。ローカルでは全体管理者だけが直接参照 |
| `system/maintenanceMode` | `active`, `message`, `scheduledEndText` | サイト全体のメンテナンス状態。`scheduledEndText` は旧画面との読み取り互換用 |

既存の `role` は `admin / staff / user` のままです。管理者の区別は `accessScope` で表します。

- 全体管理者: `role=admin`, `accessScope=global`
- 事業所管理者: `role=admin`, `accessScope=office`
- 支援員: `role=staff`, `accessScope=office`
- 利用者: `role=user`, `accessScope=self`

既存の `groupId` は将来の運営会社IDとの互換項目として残します。稼働中データの値を強制変更する処理や一括移行は追加していません。新しいローカルデータでは `organizationId` と `groupId` を同じ値にしています。

## 実装した操作

全体管理者画面に、次のローカル検証用UIを追加しました。

1. 運営会社の作成・変更
2. 運営会社に属する事業所の作成・変更
3. 運営会社と事業所を選んだ事業所管理者アカウントの発行
4. 登録済み運営会社、事業所、事業所管理者の一覧表示
5. 登録ユーザー一覧の運営会社・事業所フィルター

これらの保存処理はCloud FunctionsのCallableを経由します。Functions側で呼び出し元の `users/{uid}` を読み、`role=admin` かつ `accessScope=global` であることを再確認します。画面を直接表示するだけでは作成できません。

既存のユーザー発行Callableにも次の制限を追加しました。

- 事業所管理者は `admin` を発行できない
- 事業所管理者が送った `organizationId`、`groupId`、`officeId` は採用せず、呼び出し元管理者の所属へサーバー側で固定する
- 事業所管理者画面では会社IDと事業所IDを読み取り専用にし、管理者の選択肢と管理者アカウントを操作一覧から外す
- `既存UIDを紐づけ` は移行・復旧用として全体管理者だけに表示し、事業所管理者は新規アカウント登録と既存ユーザー編集だけを行う
- 旧 `engine_chiba` を修正する一度限りの操作は通常画面から外し、互換Callableも全体管理者だけに制限する
- 運営会社・事業所・事業所管理者の管理欄と課金安全チェックは全体管理者だけに表示する

ログイン後のヘッダーは `offices/{officeId}.name` を読み、`事業所名 スケジュール` と表示します。事業所ドキュメントや名前がまだない既存環境では、従来の `enzine スケジュール` を表示して動作を継続します。名称はユーザーへ複製せず事業所IDから取得するため、事業所名を変更しても各ユーザーの更新は不要です。

運営会社と所属データの関連付けには変更可能な会社名ではなく、固定の `organizationId`（互換用 `groupId`）を使用します。運営会社名を後から追加・変更しても、IDを変えない限り事業所やユーザーの所属は変わりません。

## メンテナンスモード

全体管理者画面に「サイトメンテナンス」を追加しました。

- 開始・解除は `role=admin` かつ `accessScope=global` の全体管理者だけが実行できる
- 状態変更は `setMaintenanceMode` Callableを使用し、Firestore直接書き込みは拒否する
- メンテナンス中も全体管理者・事業所管理者は通常どおりアクセスできる
- 支援員・利用者は `maintenance.html` へ移動し、Firestoreルールでも業務データへの読み書きを拒否する
- すでに開いている支援員・利用者画面も状態変更を検知してメンテナンス画面へ移動する
- 支援員向けChatwork Callableもメンテナンス中は拒否する
- 公開ログイン画面と `maintenance.html` は「メンテナンス中です」と任意の案内文だけを表示し、ログイン欄や管理者入口を表示しない
- 管理者は公開画面からリンクしない `maintenance-admin-login.html` を使い、通常公開中にこのURLを開いた場合は通常ログインへ戻す
- 専用URLを知っていること自体は権限とせず、Firebase Authenticationの認証後に有効な `role=admin` であることを再確認する
- `system/maintenanceMode` には公開してよい案内文だけを保存し、秘密情報は保存しない

本番で使う場合、この機能自体を通常公開中の事前リリースで導入し、全体管理者が切り替えできることを確認してからアップデート日に使用します。

1. 全体管理者の `accessScope=global` とログインを事前確認
2. メンテナンス開始
3. 支援員・利用者が案内画面へ移動することを確認
4. 本番更新・データ移行・管理者確認
5. メンテナンス解除
6. 支援員・利用者の再ログインと主要画面を確認

解除忘れを防ぐため、必要に応じて終了予定を任意の案内文へ含め、作業手順の最後に解除確認を必須項目として残します。全体管理者アカウントが未設定の状態でメンテナンスを有効にしてはいけません。

## 事業所ごとのログインロゴ案

サイトやログインHTMLを事業所ごとに複製せず、共通ログイン画面へ `?office={officeId}` を付けて事業所を識別します。ロゴは `office-logos/{officeId}.png` の固定IDをファイル名にし、ファイルがない場合は画像を表示しない方式を実装しました。URLの値はロゴ表示だけに使用し、ログイン後の所属と権限は従来どおり認証ユーザーの `users/{uid}` から決まります。

追加手順は [office-logos/README.md](office-logos/README.md) にまとめています。全体管理画面から画像をアップロードする方式は操作が簡単ですが、Firebase Storage、Storageルール、容量制限、画像形式・サイズ検査が新たに必要です。現段階では安全範囲を広げないため未実装です。

## ローカルFirestoreルール

`firestore.emulator.rules` に、会社・事業所・役割単位のアクセス制御を追加しました。

- `organizations`、`offices` の変更はFirestore直接書き込みを拒否し、全体管理者を再確認するCallableだけに限定
- `users` は本人、全体管理者、または同一事業所で管理対象となる役割だけを許可
- `monthlySchedules`、`weeklyReports`、`weeklyReportUserStatus` は利用者本人または同一会社・事業所の支援員・事業所管理者だけを許可
- `monthlyAnnouncements` とワークショップ・教材は同一会社・事業所だけを許可し、利用者からの変更は拒否
- Active利用者キャッシュを事業所ごとのドキュメントに分け、他事業所キャッシュの読み取りを拒否
- 一覧取得は会社ID、事業所ID、必要な役割で絞ったクエリだけを許可

千葉の `engine_chiba` と `enzine_chiba` の互換参照は、現在の事業所が千葉の場合だけ有効にしました。大阪など別事業所から千葉の互換IDを探索しないための制限です。

## ローカルでの確認手順

[LOCAL_FIREBASE_EMULATORS.md](LOCAL_FIREBASE_EMULATORS.md) の手順で起動・投入・検証します。

```powershell
npm run firebase:emulators
npm run firebase:seed
npm run firebase:verify
```

画面を開かず一回だけ確認する場合は `npm run firebase:test` を使えます。必要なエミュレーターを自動起動し、架空データの投入と検証後に自動停止します。

自動検証ではすべて `example.test` の架空アカウントを使い、次を確認します。

- 全体管理者による「会社作成 → 事業所作成 → 事業所管理者発行」
- 運営会社名を変更しても会社IDと事業所・事業所管理者の所属IDが維持されること
- 全体管理者だけがメンテナンスを開始・解除できること
- メンテナンス中も全体管理者・事業所管理者は利用でき、支援員・利用者の業務データとCallableが拒否されること
- 解除後に利用者の通常アクセスが戻ること
- 作成された事業所管理者の `role`、`accessScope`、会社・事業所ID
- 事業所管理者による会社作成と管理者発行の拒否
- 事業所管理者による旧事業所ID移行Callableの拒否
- 事業所管理者が不正な会社・事業所IDを送っても、支援員・利用者が本人の所属先へ固定されること
- Active利用者キャッシュが対象事業所だけに追加されること
- 未認証、他事業所、権限外のFirestoreアクセスが拒否されること
- `users`、予定、週報、お知らせ、教材、事業所別キャッシュで必要なアクセスだけが許可されること
- 条件なしの全件取得が拒否され、会社・事業所・役割で絞った一覧クエリだけが許可されること
- 既存の支援員向け認証付きCallableが引き続き動作すること

画面でも、全体管理者、事業所管理者、支援員、利用者の4種類で読み込みエラーがないことを確認済みです。全体管理者にだけ会社・事業所管理欄が表示され、事業所管理者では所属欄が固定され同一事業所の支援員・利用者だけが一覧に出ます。

## 未実装と次段階のリスク

今回の完成範囲はローカル実装の土台です。画面表示の制御だけを本番アクセス制御の完成とは扱いません。

- 会社・事業所・役割単位のルールはローカル専用の第1版です。本番ルールとの差分確認、千葉データを複製した互換テスト、段階的なロールバック計画なしに本番へ反映してはいけません。
- メンテナンス制御もローカル専用です。本番では全体管理者を先に準備し、緊急解除手順と解除忘れチェックを決めてから有効化します。
- 既存の管理画面にはFirestoreへ直接書き込む更新経路が残ります。所属境界と役割はローカルルールで防御していますが、変更可能フィールドの細かな許可リスト化とCallableへの統一は未完了です。
- 既存データで `organizationId` や `accessScope` がない場合の互換判定は残しています。本番移行時は千葉データの調査、段階的な補完、ロールバック方法が必要です。
- 予定、週一報告、お知らせ、教材、ユーザー一覧キャッシュはローカルで事業所分離を確認しました。請求安全表示とChatwork連携は、Functions側を含む本番導入前の追加監査が必要です。
- Custom Claims、監査ログ、管理者停止時の権限失効、複合インデックスは本番設計時に検討が必要です。
- 本番用 `firebase.json`、`.firebaserc`、本番Firestoreルール、本番データ、公開サイトは未変更です。本番へデプロイしていません。

次段階では、匿名化した千葉相当データで互換テストを追加し、変更可能フィールドの許可リストとCallable移行範囲を決めてから、本番ルール案を別ファイルで作るのが安全です。

本番候補のHosting配布物、Firestoreルール、データ事前監査、段階導入と緊急解除は [PRODUCTION_RELEASE_SAFETY.md](PRODUCTION_RELEASE_SAFETY.md) にまとめています。本番用 `firebase.json` と `.firebaserc` は変更せず、本番デプロイも行っていません。
