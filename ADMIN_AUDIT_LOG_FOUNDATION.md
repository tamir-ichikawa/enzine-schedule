# 管理監査ログの安全な土台

## 現在の状態

2026年8月2日、管理操作の追記型監査ログをローカルFirebase Emulator向けに実装・検証し、ユーザーの明示承認後にFunctions専用設定で本番へ反映しました。本番Functionsは候補と一致する15件です。監査ログ表示を含むHostingはまだ本番liveへ反映していません。FirestoreデータとFirestore Rulesはこの段階では変更していません。

本番Functionsの読み取り専用監査では、既存12件を維持して新規3件を追加し、削除0件であることを確認しました。Functions専用dry-runは合格し、Firestoreイベントトリガー用のEventarc・Pub/Sub関連IAMロール3件の付与も明示承認済みです。初回デプロイではIAM伝播待ちによりトリガー2件だけ失敗しましたが、伝播後に未作成2件だけを再実行し、最終的に15件すべての反映が完了しました。

反映後は、本番Functions 15件が候補と完全一致して削除0件であること、新しい `getAdminAuditLogs` を含む管理Callable 7件が未認証アクセスを `401 UNAUTHENTICATED` で拒否すること、本番の会社・事業所・29ユーザーの所属データに補完不足や警告がないことを読み取り専用で確認しました。

新しいローカル候補:

- `getAdminAuditLogs`: 全体管理者だけが直近ログを取得するCallable
- `auditManagedUserWrite`: `users/{uid}` の作成・更新・削除を記録するFirestoreトリガー
- `auditBillingSafetyWrite`: `system/billingSafety` の作成・更新・削除を記録するFirestoreトリガー

2つのFirestoreトリガーは、永続的な障害時に最長7日間の再実行と追加課金が続くことを避けるため、失敗時の自動再試行を無効にしています。監査ログの重複と課金暴走を避ける安全側の設定です。一時的な障害時には該当ログを取りこぼす可能性があるため、厳密な監査保証が必要になった段階でイベントIDによる冪等化と有限回の再処理設計を追加します。

会社・事業所・メンテナンス操作は既存Callable内で、対象データの更新と監査ログ追加を同一Firestoreバッチにしています。ユーザー情報と課金安全状態は既存画面からの直接更新もあるため、Firestore認証コンテキスト付きトリガーで補足します。

## 記録対象

- メンテナンス開始・解除
- 運営会社の作成・更新
- 事業所の作成・更新
- 事業所ID移行
- ユーザー・事業所管理者の作成・更新・削除
- 課金安全状態の作成・更新・削除

ログには、操作時刻、操作種別、操作者UID・役割・所属ID、対象種別・対象ID、変更された項目名、結果だけを保存します。氏名、メールアドレス、パスワード、メンテナンス案内文、課金安全の理由本文は保存・返却しません。

## アクセス境界

- `adminAuditLogs` はクライアントから直接読み書きできない
- ログ追加はCloud FunctionsのAdmin SDKだけが行う
- 画面への返却は `getAdminAuditLogs` が全体管理者を再確認してから行う
- 事業所管理者・支援員・利用者はCallable取得もFirestore直接取得も拒否する
- 全体管理者画面には直近50件だけを表示し、最大取得数は100件に制限する

この「追記型」はアプリケーション経由の更新に対する制約です。FirebaseプロジェクトのIAM管理者やConsole操作まで技術的に不変にするものではありません。より強い改ざん耐性が必要な場合は、別プロジェクトまたはCloud Loggingへの転送と保持ポリシーを次段階で検討します。

## ローカル検証結果

- Node安全テスト31件: 合格
- Functions候補: 許可リスト4ファイル / 15 Functionsで合格
- Hosting候補: 許可リスト24ファイルで合格
- Emulator: 全体管理者だけがログ取得可能
- Emulator: 事業所管理者のCallable取得を拒否
- Emulator: 全体管理者を含む全クライアントのFirestore直接取得を拒否
- Emulator: 会社作成・更新、事業所作成、ユーザー作成、課金安全変更、メンテナンス開始・解除を記録
- Emulator: ログ返却値にメール、パスワード、入力文章を含めない
- 既存の会社・事業所・4役割・別事業所分離・予定・週報・お知らせ・教材・Chatwork検証も継続合格

## コストと未実装

- 管理操作ごとに原則1件のFirestore書き込みが増える
- ユーザー情報と課金安全状態の変更時にFirestoreトリガーが1回動く
- ログのTTL、自動削除、長期アーカイブ、CSV出力、改ざん検知ハッシュは未実装
- ログイン成功・失敗、Chatwork送信、通常の予定・週報操作は今回の監査対象外

低頻度の管理操作だけを対象にしているため、現在規模での増加は小さい想定です。dry-runでは新規3件、Eventarc関連サービス、IAMロールの自動付与を確認しました。

## 本番反映の順序

1. 本番Functions 12件を読み取り監査し、候補15件との差分が新規3件だけであることを確認
2. Functions専用設定でdry-run（合格済み）
3. Eventarc・Pub/Sub用IAMロール3件の自動付与を含むユーザーの明示承認後、Functionsだけを反映（完了）
4. 未認証Callable拒否と既存Functionsを確認（15件一致・削除0件を確認済み）
5. Hosting専用プレビューへ全体管理者画面を反映
6. 全体管理者でログ表示、事業所管理者で非表示を実機確認
7. 別の明示承認後にHosting liveを更新

Firestore Rulesの再反映は不要です。現在の末尾拒否ルールにより `adminAuditLogs` のクライアント直接アクセスはすでに拒否されます。

## 期限付きHostingプレビュー

2026年8月2日、監査ログUIを既存の `multi-office-20260802` プレビューチャンネルへHosting専用設定で反映しました。期限は2026年8月3日20時31分頃（日本時間）です。公開24ファイルの許可リスト監査、主要4ファイルのHTTP 200・SHA-256一致、必須セキュリティヘッダー、本番 `live` チャンネルの版が変わっていないことを確認しました。

本番 `live` 反映前に、全体管理者で監査ログ欄が表示・取得できること、事業所管理者では監査ログ欄が非表示であることをプレビュー上で確認します。

初回確認では、事業所管理者でも監査ログ欄の外枠が表示されるCSS競合が見つかりました。権限判定とCallable拒否は正常で、後段の `display: flex` が `.hidden` を見た目だけ上書きしていたことが原因です。監査ログ欄専用の `display: none !important` とCSSキャッシュ更新を追加し、31テスト合格後に同じプレビューだけを更新しました。修正版の期限は2026年8月3日20時38分頃（日本時間）で、主要4ファイルのSHA-256一致、セキュリティヘッダー、本番 `live` 版が不変であることを確認済みです。

全体管理者で表示、事業所管理者で非表示であることをユーザーが実機確認した後、明示承認を受けてHosting専用設定で本番 `live` へ反映しました。反映後のlive版は `3dfd9043bbf9cf2d` で、主要4ファイルのHTTP 200・SHA-256一致と必須セキュリティヘッダー、本番Functions 15件・削除0件の維持を確認しました。Functions、Firestore Rules、本番データ、メンテナンス状態はこのHosting反映では変更していません。

本番 `live` 反映後、全体管理者と事業所管理者の表示をユーザーが実機確認し、問題なしとの報告を受けました。管理監査ログ基盤のFunctions・Hosting反映と役割別表示確認は完了です。
